# ============================================================================
# Sigma9 Client Configuration
# ============================================================================
# 이 파일은 GUI 클라이언트(Frontend) 전용 설정입니다.
# Backend 서버에서는 이 파일을 참조하지 않습니다.
#
# 📌 서버 주소 변경:
#   - 로컬 개발: host: "localhost"
#   - AWS 배포 후: host: "<EC2 Public IP>" 또는 도메인
# ============================================================================

# ═══════════════════════════════════════════════════════════════════════════
# Server Connection (서버 연결 설정)
# ═══════════════════════════════════════════════════════════════════════════
server:
  host: "localhost"             # 서버 호스트 (AWS 배포 시 변경)
  port: 8000                    # 서버 포트
  ws_path: "/ws/feed"           # WebSocket 엔드포인트 경로
  api_path: "/api"              # REST API 기본 경로
  use_ssl: false                # HTTPS/WSS 사용 여부 (프로덕션에서 true)

# ═══════════════════════════════════════════════════════════════════════════
# Connection Behavior (연결 동작)
# ═══════════════════════════════════════════════════════════════════════════
connection:
  auto_connect: true            # GUI 시작 시 자동 연결
  reconnect_enabled: true       # 연결 끊김 시 자동 재연결
  reconnect_interval: 5         # 재연결 시도 간격 (초)
  max_reconnect_attempts: 10    # 최대 재연결 시도 횟수 (0 = 무제한)
  timeout: 30                   # 연결/요청 타임아웃 (초)
  heartbeat_interval: 15        # WebSocket 헬스체크 간격 (초)

# ═══════════════════════════════════════════════════════════════════════════
# GUI Settings (GUI 설정)
# ═══════════════════════════════════════════════════════════════════════════
gui:
  theme: "dark"                 # 테마 (dark/light)
  window_opacity: 0.95          # 윈도우 투명도 (0.0 ~ 1.0)
  acrylic_alpha: 180            # Acrylic 블러 알파값 (0 ~ 255)
  particle_opacity: 0.6         # 파티클 이펙트 투명도
  background_effect: "constellation" # 배경 파티클 이펙트 (constellation, digital dust, bokeh, vector field, matrix rain, none)
  tint_color: "#1a1a2e"         # Acrylic 틴트 색상
  remember_window_position: true  # 윈도우 위치 기억
  confirm_kill_switch: true     # Kill Switch 실행 전 확인창

# ═══════════════════════════════════════════════════════════════════════════
# Chart Settings (차트 설정)
# ═══════════════════════════════════════════════════════════════════════════
chart:
  default_timeframe: "1D"       # 기본 타임프레임
  show_volume: true             # 거래량 표시
  show_vwap: true               # VWAP 오버레이
  show_indicators: true         # 지표 표시 여부
  max_bars: 500                 # 최대 캔들 개수 (메모리 최적화)

# ═══════════════════════════════════════════════════════════════════════════
# Logging Settings (클라이언트 로깅)
# ═══════════════════════════════════════════════════════════════════════════
logging:
  level: "INFO"                 # 로그 레벨
  console_visible: true         # 콘솔 패널 기본 표시 여부
  max_console_lines: 500        # 콘솔 최대 라인 수
