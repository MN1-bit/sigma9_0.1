# ============================================================================
# Seismograph Strategy Package - 매집 탐지 + 폭발 포착 전략
# ============================================================================
# 📌 패키지 구조:
#   seismograph/
#   ├── __init__.py       # 진입점 (이 파일)
#   ├── strategy.py       # SeismographStrategy 클래스 [03-002]
#   ├── models.py         # TickData, WatchlistItem 데이터 모델
#   ├── scoring/          # 점수 계산 모듈 (v1, v2, v3)
#   └── signals/          # 시그널 탐지 모듈
#
# 📖 사용 예시:
#   >>> from backend.strategies.seismograph import SeismographStrategy
#   >>> strategy = SeismographStrategy()
#
# 📖 리팩터링:
#   - [03-001] 패키지 구조 + 모듈 분리
#   - [03-002] 완전 마이그레이션 (signals/, scoring/ 실제 사용)
#   - 원본 백업: docs/archive/seismograph_backup.py
# ============================================================================

"""
Seismograph Strategy Package

미국 마이크로캡 주식에서 세력의 매집(Accumulation)을 사전 탐지하고,
폭발 순간(Ignition)을 포착하는 2단계 전략입니다.

[03-002] 완전 마이그레이션
- strategy.py: SeismographStrategy 클래스 (~290줄)
- signals/: 시그널 강도 계산 함수
- scoring/: 점수 계산 함수 (V1, V2, V3)
"""

# ═══════════════════════════════════════════════════════════════════════════
# [03-002] 새 모듈에서 import
# ═══════════════════════════════════════════════════════════════════════════

from .strategy import SeismographStrategy
from backend.models import TickData, WatchlistItem

# 외부에서 볼 수 있는 public API
__all__ = [
    "SeismographStrategy",
    "TickData",
    "WatchlistItem",
]
