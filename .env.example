# ============================================
# Sigma9 환경 변수 템플릿
# ============================================
# 사용법:
# 1. 이 파일을 복사하여 .env 파일로 저장
# 2. 본인의 IBKR 계정 정보로 수정
# 3. .env 파일은 절대 Git에 커밋하지 마세요!
# ============================================

# === IBKR 연결 설정 ===
# IB Gateway가 실행 중이어야 합니다
IB_HOST=127.0.0.1
IB_PORT=4002               # IB Gateway Paper: 4002, Live: 4001
IB_CLIENT_ID=1             # 고유 클라이언트 ID (중복 불가)

# === 계좌 정보 (선택) ===
IB_ACCOUNT=                # Paper Trading 계좌 ID (비워두면 자동 감지)

# === Polygon.io API ===
# https://polygon.io/dashboard 에서 발급
POLYGON_API_KEY=your_polygon_api_key_here

# === LLM API (선택) ===
# OpenAI 또는 Anthropic 중 하나만 설정
OPENAI_API_KEY=            # OpenAI API 키
ANTHROPIC_API_KEY=         # Anthropic API 키
