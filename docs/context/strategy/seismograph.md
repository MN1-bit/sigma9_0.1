# Seismograph 전략 가이드

> **버전**: v3.2 (2026-01-09)  
> **철학**: "매집을 탐지하고, 폭발 순간에 진입하고, 급등을 수확한다."

---

## 1. 전략 개요

Seismograph는 미국 **마이크로캡 주식**에서 **세력의 매집(Accumulation)**을 사전 탐지하고, **폭발 순간(Ignition)**을 포착하여 진입하는 2단계 전략입니다.

이름의 유래: 큰 지진(급등) 전에는 미세한 진동(매집 신호)이 있습니다. 이 전략은 지진계(Seismograph)처럼 그 진동을 감지합니다.

---

## 2. 계층 구조: Scan → Tier1 → Tier2 → Trade

Seismograph는 **깔때기(Funnel)** 구조로 종목을 단계적으로 필터링합니다.

```
   ┌─────────────────────────────────────┐
   │         전체 시장 (~8,000 종목)       │  ← Universe
   └───────────────┬─────────────────────┘
                   ▼ [Universe 필터]
   ┌─────────────────────────────────────┐
   │       스캔 대상 (~500 종목)          │  ← $2-$10, 마이크로캡
   └───────────────┬─────────────────────┘
                   ▼ [Score V3 계산]
   ┌─────────────────────────────────────┐
   │     Tier 1 Watchlist (~50 종목)      │  ← Score ≥ 60
   └───────────────┬─────────────────────┘
                   ▼ [Stage 4 승격]
   ┌─────────────────────────────────────┐
   │       Tier 2 Hot Zone (~5 종목)      │  ← TR > 0.5 (폭발 임박)
   └───────────────┬─────────────────────┘
                   ▼ [Ignition 감지]
   ┌─────────────────────────────────────┐
   │          실제 매매 (1~2 종목)         │  ← Ignition ≥ 70
   └─────────────────────────────────────┘
```

### 2.1 각 계층의 역할

| 계층 | 종목 수 | 조건 | 데이터 소스 | 업데이트 주기 |
|------|---------|------|-------------|---------------|
| **Universe** | ~8,000 | 미국 전체 주식 | - | - |
| **Scan 대상** | ~500 | 가격/시총/유동성 필터 | 일봉 | 1일 |
| **Tier 1** | ~50 | Score V3 ≥ 60 | 일봉 | 1일 |
| **Tier 2** | ~5 | Stage 4 (TR > 0.5) | 실시간 | 1초 |
| **Trade** | 1~2 | Ignition ≥ 70 | 틱 | 실시간 |

### 2.2 계층 전이 상세

#### Scan → Tier 1 (Watchlist 진입)

매일 아침 장 시작 전, 모든 스캔 대상 종목에 대해 **Score V3**를 계산합니다.

- **진입 조건**: Score V3 ≥ 60
- **의미**: "매집 신호가 충분히 강한 종목"
- **모니터링**: 1분봉 AM 채널 구독, 1초마다 Watchlist 브로드캐스트

#### Tier 1 → Tier 2 (Hot Zone 승격)

Watchlist 종목 중 **변동성 수축**이 감지되면 Hot Zone으로 승격됩니다.

- **승격 조건**: `Tight Range Intensity > 0.5` (= Stage 4)
- **의미**: "폭발 직전 상태, 실시간 모니터링 필요"
- **모니터링**: T 채널(틱) 구독, 실시간 Ignition Score 계산

#### Tier 2 → Trade (매매 실행)

Hot Zone 종목에서 **거래량 폭발**이 감지되면 매수합니다.

- **진입 조건**: Ignition Score ≥ 70
- **의미**: "폭발 시작, 지금 진입해야 함"
- **실행**: OCA 주문 (Entry + Stop Loss + Target)

### 2.3 왜 이 구조인가?

| 문제 | 해결책 |
|------|--------|
| 8,000개 종목을 실시간 감시 불가 | → 일봉 스캔으로 50개로 축소 |
| 50개도 틱 감시는 부하 높음 | → Stage 4인 5개만 틱 구독 |
| "언제" 진입할지 모름 | → Ignition Score로 타이밍 포착 |

---

## 3. Universe 필터 조건

스캔 대상을 선정하기 위한 1차 필터입니다.

| 조건 | 범위 | 이유 |
|------|------|------|
| 주가 | $2 ~ $10 | 마이크로캡 타겟 |
| 시가총액 | $50M ~ $300M | 세력 개입 가능 규모 |
| 유통주식수 | ≤ 15M | Float 적어야 급등 용이 |
| 평균 거래량 | ≥ 100K | 유동성 확보 |
| 당일 변화율 | 0% ~ 5% | 아직 폭발 전 상태 |

---

## 4. 4가지 매집 신호

주가가 급등하기 전, 세력은 흔적을 남깁니다. Seismograph는 4가지 신호로 이를 탐지합니다.

| 신호 | 무엇을 보는가? | 가중치 |
|------|---------------|--------|
| **Tight Range** | 변동성이 수축하는가? (폭풍 전 고요) | 35% |
| **Absorption** | 거래량 대비 가격 반응이 작은가? (매수 흡수) | 30% |
| **Accumulation Bar** | 조용히 양봉 마감이 반복되는가? | 20% |
| **Volume Dryout** | 거래량이 점진적으로 감소하는가? | 15% |

---

## 5. Score V3.2 알고리즘

### 5.1 기본 점수 계산

$$
\text{Base Score} = \sum_{i} (\text{Intensity}_i \times \text{Weight}_i) \times 100
$$

각 신호의 강도(Intensity)는 0.0 ~ 1.0 범위입니다.

### 5.2 Harmony Bonus (시그널 조합 보너스)

여러 신호가 동시에 강하면 추가 점수를 부여합니다:

$$
\text{Harmony Bonus} = \begin{cases}
+10 & \text{if } I_{TR} > 0.6 \text{ AND } I_{OBV} > 0.5 \\
+5 & \text{if 3개 이상 시그널} > 0.5 \\
0 & \text{otherwise}
\end{cases}
$$

### 5.3 Redundancy Penalty (죽은 압축 필터)

변동성 수축만 있고 매수 흡수가 없으면 "죽은 압축"으로 판단하여 감점합니다:

$$
\text{Redundancy Penalty} = \begin{cases}
0.5 & \text{if } I_{TR} > 0.7 \text{ AND } I_{OBV} < 0.3 \text{ AND } I_{AB} < 0.3 \\
1.0 & \text{otherwise}
\end{cases}
$$

### 5.4 최종 점수

$$
\text{Final Score} = \text{clip}(\text{Base} + \text{Harmony}, 0, 100) \times \text{Redundancy}
$$

---

## 6. 개별 신호 상세

### 6.1 Tight Range (변동성 수축, 35%)

**원리**: ATR(Average True Range)이 역사적 수준 대비 낮으면 폭발 임박

**V3.2 정규화**: Percentile 기반

$$
\text{Percentile} = \frac{\text{현재 ATR보다 작은 값 개수}}{60\text{일 ATR 개수}}
$$

$$
I_{TR} = 1 - \text{Percentile}
$$

ATR이 60일 중 가장 낮은 수준이면 $I_{TR} \approx 1.0$ (폭발 임박)

**해석**:
- 0.8+ = 매우 좁음 → **Tier 2 승격 대상** 🔥
- 0.5+ = Stage 4 진입
- 0.5 미만 = 아직 대기

### 6.2 Absorption (매수 흡수, 30%)

**원리**: 거래량은 많은데 가격이 움직이지 않으면 → 누군가 물량을 흡수 중

**계산**:

$$
\text{Signed Volume} = \sum_{t=-10}^{-1} \text{sign}(r_t) \times V_t
$$

$$
\text{SV}_{norm} = \frac{\text{Signed Volume}}{\text{Median Volume}}
$$

- $\text{SV}_{norm} \leq 0$: 매도 우세 → 강도 0.0 ~ 0.5
- $\text{SV}_{norm} > 0$: 매수 우세 → 강도 0.5 ~ 1.0

### 6.3 Accumulation Bar (매집봉, 20%)

**원리**: 조용한 상승 = 세력의 은밀한 매집

$$
I_{AB} = 0.5 + \text{Adjustment}
$$

| 요소 | 조건 | 가감점 |
|------|------|--------|
| 양봉 비율 | ≥60% | +0.15 |
| 양봉 비율 | ≤40% | -0.15 |
| 상단 마감 비율 | ≥70% | +0.10 |

### 6.4 Volume Dryout (거래량 마름, 15%)

**원리**: 거래량 급감 = "폭풍 전 고요"

$$
I_{VD,base} = 1 - \frac{\text{최근 3일 평균}}{\text{20일 평균} \times 0.4}
$$

**V3.2**: 가격이 레인지 하단이면 추가 감점 (지지 실패)

---

## 7. Stage 결정과 Tier 2 승격

### 7.1 Stage 정의

| Stage | 조건 | 의미 | Tier |
|-------|------|------|------|
| **1** | 기본 | 모니터링 대상 | Tier 1 |
| **2** | $I_{OBV} > 0.5$ | 매수 흡수 감지 | Tier 1 |
| **4** | $I_{TR} > 0.5$ | 변동성 수축 = 폭발 임박 | **Tier 2** 🔥 |

### 7.2 Tier 2 승격 조건

$$
\text{Tier 2 승격} \Leftrightarrow \text{Stage} = 4 \Leftrightarrow I_{TR} > 0.5
$$

Tier 2로 승격되면:
- T 채널(틱) 구독 시작
- 실시간 Ignition Score 계산
- GUI에서 "Hot Zone" 표시

---

## 8. Ignition 감지와 Trade 실행

### 8.1 Ignition Score

Tier 2 종목에서 실시간 틱 데이터 기반으로 "폭발 순간"을 감지합니다.

$$
\text{Volume Burst} = \frac{\text{최근 10틱 거래량}}{\text{이전 50틱 평균 거래량}}
$$

$$
\text{Ignition Score} = \min(100, \text{Volume Burst} \times 20)
$$

### 8.2 Trade 진입 조건

$$
\text{Ignition Score} \geq 70 \Rightarrow \text{BUY 신호 발생}
$$

추가 조건:
- 스프레드 ≤ 1.0%
- 개장 후 15분 경과
- can_trade = (Stage ≥ 4) OR (Score V3 > 50)

---

## 9. 파라미터 설정

### Tier 1 → Tier 2 승격

| 파라미터 | 기본값 | 설명 |
|----------|--------|------|
| Watchlist 진입 Score | 60 | Tier 1 진입 기준 |
| Stage 4 TR Threshold | 0.5 | Tier 2 승격 기준 |

### Tier 2 → Trade 실행

| 파라미터 | 기본값 | 설명 |
|----------|--------|------|
| Ignition Threshold | 70 | BUY 신호 기준 |
| Max Spread | 1.0% | 스프레드 상한 |
| Min Minutes After Open | 15분 | 개장 후 대기 시간 |

### Exit 관리

| 파라미터 | 기본값 | 설명 |
|----------|--------|------|
| Stop Loss | -5.0% | 손절 |
| Trail Activation | +3.0% | 트레일링 시작점 |
| Trail Amount | ATR × 1.5 | 트레일링 간격 |

---

## 10. 점수 해석

| 점수 | 의미 | Tier | 액션 |
|------|------|------|------|
| **80+** | 폭발 임박 | Tier 2 | Ignition 대기 |
| **60-80** | 강한 매집 신호 | Tier 1 | Stage 4 대기 |
| **40-60** | 일부 신호 탐지 | (제외) | - |
| **0-40** | 약함 | (제외) | - |
| **-1** | 데이터 부족 | (제외) | 신규/IPO |

---

## 11. 요약: 종목의 여정

```
1. 아침 스캔 → SMXT 발견, Score 72점 → Tier 1 진입

2. 3일 후 → TR = 0.65 (Stage 4) → Tier 2 승격, 틱 모니터링 시작

3. 09:45 AM → 거래량 폭발, Ignition 85점 → BUY 신호

4. 매수 체결 $4.50 → Stop $4.28, Target $4.95

5. +8% 상승 → Trail Stop 활성화 → 최종 청산 +6.5%
```

---

> **"Smart money leaves footprints. We just need to read them."**
