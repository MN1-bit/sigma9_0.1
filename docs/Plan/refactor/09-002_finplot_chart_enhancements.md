# Intraday 리샘플링 인프라 구축

> **상태**: 📋 **구현 계획** (USER 승인 대기)
> **작성일**: 2026-01-10
> **예상 작업 시간**: 6h
> **레이어**: Backend (`backend.data`) + Frontend (`frontend.gui`)

---

## 1. 목표

1. **On-demand 리샘플링**: 요청 시에만 3m/5m/15m/4h/1W 생성 + Parquet 저장
2. **GUI 수동 일괄 리샘플**: Settings에서 전체 티커 일괄 리샘플 (Progress + Pause/Resume)
3. **Gap-Fill 메커니즘**: 누락 데이터 자동 감지 및 보충
4. **차트 스크롤 동적 로딩**: viewport 변경 시 추가 데이터 로드
5. **에러 로깅**: 리샘플링 실패/누락 시 로그 출력

### 1.1 설계 결정: On-demand + 캐싱

> **핵심**: 요청 시 리샘플 → 저장 → 이후 직접 로드. GUI에서 수동 일괄 처리 옵션 제공.

```
┌────────────────────────────────────────────────────────┐
│                    On-demand 흐름                       │
│  Chart/Backend → 5m 요청 → 없으면 리샘플 + 저장        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│            GUI 수동 일괄 리샘플 (Settings)              │
│  [▶ Start] [⏸ Pause] [⏹ Stop]                          │
│  ████████████░░░░░░░░ 60% (12,000/20,000)              │
│  □ 실시간 전역 리샘플 (새 데이터 수집 시 자동 리샘플)   │
└────────────────────────────────────────────────────────┘
```

### 1.2 GUI 리샘플 기능 명세

| 컴포넌트 | 기능 |
|----------|------|
| **Start 버튼** | 전체 티커 일괄 리샘플 시작 |
| **Pause 버튼** | 일시 정지 (현재 진행 상태 유지) |
| **Stop 버튼** | 완전 중단 + 진행 상태 초기화 |
| **Resume** | Pause 후 이어서 진행 |
| **Progress Bar** | 현재/전체 티커 수, 퍼센티지 표시 |
| **실시간 전역 리샘플 체크박스** | 활성화 시: 새 1m/1h 데이터 수집 시 자동으로 파생 TF 리샘플 |

### 1.3 실시간 차트 리샘플 업데이트

> **핵심**: 파생 타임프레임(5m, 15m 등) 차트 표시 중에도 **최신 봉이 실시간으로 업데이트**.

```
┌────────────────────────────────────────────────────────┐
│              실시간 5m 차트 업데이트                    │
│                                                        │
│  WebSocket AM.* (1분봉) 수신                            │
│          ↓ (1초 rate)                                  │
│  현재 5분봉 = 최근 1m 데이터 리샘플 (미완료 봉)         │
│          ↓                                             │
│  차트 마지막 캔들만 업데이트 (전체 리로드 X)            │
└────────────────────────────────────────────────────────┘
```

| 설정 | 값 |
|------|---|
| 업데이트 주기 | **1초** |
| 대상 | 차트에 표시 중인 티커의 현재 (미완료) 봉 |
| 트리거 | AM.* (1분봉) WebSocket 이벤트 |
| 리샘플 소스 | 메모리 버퍼 (Parquet 재로드 X) |

| 특성 | 값 |
|------|---|
| 첫 요청 | 리샘플 발생 (~50ms) |
| 이후 요청 | Parquet 직접 로드 (~10ms) |
| 저장 위치 | `data/parquet/intraday/{tf}/{ticker}.parquet` |

---

## 2. 레이어 체크 (REFACTORING.md §2-§3)

- [x] **레이어 규칙 위반 없음**
  - `ParquetManager` → `backend.data` (하위 레이어)
  - `ChartDataService` → `frontend.services` (Frontend 내부)
  - Backend ↔ Frontend 직접 import 없음 ✅
- [x] **순환 의존성 없음** - 신규 코드, 기존 모듈과 의존관계 단방향
- [x] **DI Container 등록 필요**: **아니오**
  - `ParquetManager`는 이미 Container 등록됨 (11-001)
  - `IntradayResampler`는 스크립트 전용, Container 불필요

---

## 3. 기존 솔루션 검색 결과 (REFACTORING.md §5)

| 솔루션 | 출처 | 채택 여부 | 사유 |
|--------|------|----------|------|
| `pandas.DataFrame.resample()` | pandas | ✅ 채택 | 표준 OHLCV 리샘플링 함수 |
| `finplot` viewport 콜백 | pyqtgraph | ✅ 채택 | `sigXRangeChanged` 시그널 재사용 |
| third-party 리샘플링 라이브러리 | PyPI | ❌ 미채택 | pandas 내장 기능으로 충분 |

> ✅ 검색 완료: pandas 내장 `resample()` 사용, 별도 라이브러리 불필요

---

## 4. 아키텍처

### 4.1 디렉터리 구조 (archt.md §4.4 준수)

```
data/parquet/intraday/          # ← 기존 구조 유지 (archt.md 참조)
├── 1m/{ticker}.parquet         # API 원본 (수집 스크립트)
├── 1h/{ticker}.parquet         # API 원본 (수집 스크립트)
├── 3m/{ticker}.parquet         # 리샘플링 생성
├── 5m/{ticker}.parquet         # 리샘플링 생성
├── 15m/{ticker}.parquet        # 리샘플링 생성
├── 4h/{ticker}.parquet         # 리샘플링 생성
└── 1W/{ticker}.parquet         # 리샘플링 생성
```

### 4.2 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                      ParquetManager                             │
│  get_intraday_bars(ticker, tf, auto_fill=True)                  │
│  save_intraday_bars(ticker, tf, df)                            │
└─────────────────────────────────────────────────────────────────┘
                              ↑
┌─────────────────────────────────────────────────────────────────┐
│              IntradayResampler (스크립트 전용)                   │
│  resample_ticker(ticker, target_tf, log_missing=True)          │
│  resample_all(target_tf)                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↑
┌─────────────────────────────────────────────────────────────────┐
│                     ChartDataService                            │
│  get_chart_data(ticker, tf)                                    │
│    → ParquetManager.get_intraday_bars() 호출                    │
└─────────────────────────────────────────────────────────────────┘
                              ↑
┌─────────────────────────────────────────────────────────────────┐
│                    FinplotChartWidget                           │
│  ax.vb.sigXRangeChanged → _on_viewport_changed                 │
│    → viewport_data_needed emit (150ms debounce)                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. Gap-Fill 메커니즘

### 5.1 Backend Gap-Fill (데이터 요청 시)

```python
# ParquetManager
RESAMPLE_RULES = {
    "3m": ("1m", "3T"),
    "5m": ("1m", "5T"),
    "15m": ("1m", "15T"),
    "4h": ("1h", "4H"),
    "1W": ("1D", "W-FRI"),
}

def get_intraday_bars(self, ticker: str, tf: str, auto_fill: bool = True):
    path = self.intraday_dir / tf / f"{ticker}.parquet"
    
    if not path.exists():
        if auto_fill:
            logger.warning(f"[GAP-FILL] {ticker}/{tf} 파일 없음, 리샘플링 시도")
            return self._try_resample(ticker, tf)
        return pd.DataFrame()
    
    return pd.read_parquet(path)

def _try_resample(self, ticker: str, tf: str) -> pd.DataFrame:
    source_tf, rule = RESAMPLE_RULES.get(tf, (None, None))
    if not source_tf:
        logger.error(f"[GAP-FILL] {tf}에 대한 리샘플 규칙 없음")
        return pd.DataFrame()
    
    source_df = self.get_intraday_bars(ticker, source_tf, auto_fill=False)
    if source_df.empty:
        logger.error(f"[GAP-FILL] {ticker}/{source_tf} 소스 데이터 없음")
        return pd.DataFrame()
    
    resampled = self._resample_df(source_df, rule)
    self.save_intraday_bars(ticker, tf, resampled)
    return resampled
```

### 5.2 차트 스크롤 시 동적 로딩

```python
# FinplotChartWidget
def _setup_viewport_handler(self):
    self._viewport_timer = QTimer()
    self._viewport_timer.setSingleShot(True)
    self._viewport_timer.setInterval(150)  # 150ms 디바운스
    self._viewport_timer.timeout.connect(self._check_viewport_data_needs)
    
    # finplot ax의 ViewBox에서 시그널 연결
    self.ax.vb.sigXRangeChanged.connect(self._on_viewport_changed)
```

---

## 6. 로깅 설계

| 상황 | 레벨 | 메시지 예시 |
|------|------|------------|
| 리샘플링 시작 | INFO | `[RESAMPLE] AAPL 1m→5m 시작` |
| 리샘플링 성공 | INFO | `[RESAMPLE] AAPL 5m 저장 (500 bars)` |
| 소스 파일 없음 | WARNING | `[GAP-FILL] AAPL/1m 소스 없음` |
| 저장 실패 | ERROR | `[RESAMPLE] AAPL/5m 저장 실패: {exception}` |

**일괄 실행 리포트**: `logs/resample_failures.log` 저장

---

## 7. 변경 파일

| 파일 | 유형 | 예상 라인 | 설명 |
|------|-----|----------|------|
| `backend/data/parquet_manager.py` | MODIFY | +80 | `get_intraday_bars(tf)`, `_try_resample()`, `resample_all_tickers()` |
| `frontend/gui/chart/finplot_chart.py` | MODIFY | +60 | viewport 시그널 + 실시간 봉 업데이트 |
| `frontend/gui/panels/resample_panel.py` | **NEW** | ~150 | 리샘플 컨트롤 패널 (Progress, Pause/Resume/Stop) |
| `frontend/services/chart_data_service.py` | MODIFY | +30 | gap-fill + 실시간 리샘플 |

---

## 8. 실행 단계

### Step 1: ParquetManager 확장 (1.5h)
- `get_intraday_bars(ticker, tf, auto_fill=True)`
- `_try_resample()`, `_resample_df()` 내부 메서드
- `resample_all_tickers(target_tf, callback)` - 일괄 처리 + 콜백
- 로깅 (`loguru`)

### Step 2: ResamplePanel GUI 구현 (2h)
- Start/Pause/Stop/Resume 버튼
- QProgressBar (현재/전체 + %)
- "실시간 전역 리샘플" 체크박스
- Worker 스레드 (QThread) + 시그널로 진행 상태 전달

### Step 3: 실시간 차트 리샘플 업데이트 (1.5h)
- AM.* WebSocket 1분봉 수신 시 → 메모리 버퍼에 누적
- 1초 QTimer로 현재 파생 봉 계산 (5m = 최근 1~5분 데이터)
- `FinplotChartWidget._update_latest_candle()` - 마지막 봉만 업데이트
- 전체 차트 리로드 없이 **인크리멘탈** 업데이트

### Step 4: FinplotChartWidget viewport 연결 (0.5h)
- `sigXRangeChanged` 연결
- `viewport_data_needed` emit

### Step 5: 검증 + 문서화 (0.5h)

---

## 9. 검증 계획

### 9.1 린트 검증 (필수)

```powershell
# import-linter (REFACTORING.md §5)
lint-imports

# ruff
ruff check backend/data/parquet_manager.py

# pydeps 순환 체크 (신규 모듈 추가 시)
pydeps backend.data --only backend --show-cycles --no-output
```

### 9.2 On-demand 리샘플링 검증

```python
# 5m 파일이 없는 상태에서 요청 → 자동 생성 확인
from backend.data.parquet_manager import ParquetManager
pm = ParquetManager()
df = pm.get_intraday_bars("AAPL", "5m", auto_fill=True)

assert not df.empty  # 1m에서 리샘플되어 반환
assert (pm.intraday_dir / "5m" / "AAPL.parquet").exists()  # 저장 확인
```

### 9.3 Gap-Fill 검증

```python
# auto_fill 테스트
from backend.data.parquet_manager import ParquetManager
pm = ParquetManager()
df = pm.get_intraday_bars("AAPL", "5m", auto_fill=True)
assert not df.empty
```

### 9.4 차트 스크롤 검증 (수동)

1. 대시보드 실행: `python -m frontend.main`
2. A~M 종목 선택 (AAPL 추천) → 5m 클릭
3. 차트 왼쪽으로 스크롤 → 로그에 `[INFO] 📊 Loading more data` 확인

---

## 10. 다음 단계

- [ ] 사용자 승인
- [ ] `/IMP-execution`
- [ ] → **09-003** (타임프레임 버튼 + 테마 GUI) 진행
