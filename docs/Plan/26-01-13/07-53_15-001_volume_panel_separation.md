# 15-001: 볼륨패널 분리

> **작성일**: 2026-01-13 | **예상**: 30분

---

## 1. 목표

- 볼륨 차트를 캔들 차트 아래 별도 패널로 분리
- X축 스크롤/줌 동기화 유지

---

## 2. 레이어 체크

- [x] 레이어 규칙 위반 없음 (frontend 내부 변경)
- [x] 순환 의존성 없음
- [ ] DI Container 등록 필요: **아니오**

---

## 3. 기존 솔루션 검색 결과

| 솔루션 | 출처 | 채택 여부 | 사유 |
|--------|------|----------|------|
| `fplt.create_plot(rows=2)` | finplot 내장 | ✅ 채택 | 공식 multi-row 패턴 |

---

## 4. 변경 파일

| 파일 | 유형 | 예상 라인 |
|------|-----|----------|
| `frontend/gui/chart/finplot_chart.py` | MODIFY | +10 |

---

## 5. 실행 단계

### Step 1: `_setup_ui()` 수정

**변경 전 (L153-159)**:
```python
self.ax = fplt.create_plot(init_zoom_periods=100)
self.ax_volume = self.ax.overlay()  # ← 오버레이
```

**변경 후**:
```python
# 📌 [15-001] 분리된 2-row 레이아웃
self.ax, self.ax_volume = fplt.create_plot(rows=2, init_zoom_periods=100)

# 볼륨 패널 높이 비율 (25%)
self.ax_volume.setHeight(0.25)
```

### Step 2: 레이아웃 추가 코드 확인

- `layout.addWidget(self.ax.vb.win, stretch=1)` ← 변경 불필요 (자동 포함)

---

## 6. 검증

### 자동 테스트
```bash
ruff check frontend/gui/chart/finplot_chart.py
```

### 수동 테스트
1. 앱 실행 → 차트 렌더링 확인
2. 볼륨 바가 하단 별도 패널에 표시
3. 스크롤/줌 시 두 패널 동기화 확인

---

**다음**: `/IMP-execution`
