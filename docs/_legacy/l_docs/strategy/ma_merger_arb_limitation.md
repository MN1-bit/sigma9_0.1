# M&A/Merger Arbitrage 종목의 구조적 한계 분석

> **작성일**: 2026-01-06  
> **사례**: FOLD (12/18~19 급등)  
> **상태**: 📝 분석 문서

---

## 1. 현상 분석

### 1.1 FOLD 사례

| 항목 | 내용 |
|------|------|
| 이벤트 | 대형 M&A 인수 공시 (12/18) |
| 급등 원인 | 실적/임상 결과 아님, **단일 이벤트 가격 재조정** |
| 이후 성격 | 바이오 모멘텀 플레이 → **머저아브(Merger Arb) 스프레드** |
| 가격 특성 | 딜 깨지지 않는 한 **인수가 근처에서 횡보** |

### 1.2 왜 상위권에 노출되는가?

```
M&A 공시 직후:
├── 급등 → Volume Spike (일시적)
├── 인수가 근처 횡보 → Tight Range (지속)
├── 매수세 지속 → OBV/Absorption 상승 (지속)
└── 변동성 감소 → Volume Dryout (지속)
```

**결론**: 4가지 매집 신호 중 3개 이상이 **구조적으로 충족**됨

---

## 2. 구조적 한계

### 2.1 Score V3 설계 전제

| 전제 | M&A 종목 특성 | 불일치 |
|------|---------------|--------|
| "조용한 매집 후 폭발" | 이미 폭발 완료, 인수가 고정 | ❌ |
| "가격 상승 여력 존재" | 인수가가 상한선 | ❌ |
| "변동성 수축 = 압축" | 변동성 감소 = 딜 확정 신뢰 | ❌ |

### 2.2 거짓 양성 패턴

```
Score V3가 탐지하는 것:
  "조용한 매집 → 폭발 직전"

M&A 종목이 보여주는 것:
  "이미 폭발 완료 → 인수가 수렴 중"
  
둘 다 같은 신호 패턴을 생성하지만 의미가 완전히 다름
```

---

## 3. 개선 전략

### 3.1 Option A: 이벤트 기반 필터 (데이터 의존)

**개념**: M&A/인수 공시 종목 자동 제외

```python
# 외부 데이터 필요
if ticker in ma_announced_list:
    return 0.0  # 또는 별도 태그
```

**장점**: 정확한 필터링  
**단점**: 외부 데이터 소스 필요, 실시간 업데이트 어려움

---

### 3.2 Option B: ATR Phase Shift 탐지 (⭐ 권장)

**개념**: 3단계 ATR 비교로 "이벤트 후 고착" 패턴 정밀 탐지

```
진정한 매집 (VCP):
├── 기존 ATR      : 보통
├── 이벤트       : 없음 (점진적 압축)
└── 이후 ATR     : 점진적 감소

M&A/이벤트 종목:
├── 기존 ATR      : 보통~높음
├── 이벤트       : 급등/급락 (1일 ±30% 이상)
└── 이후 ATR     : 급격히 낮음 (인수가 고착)
```

**핵심 지표: ATR Collapse Ratio**

```python
def detect_atr_phase_shift(data, lookback=40):
    """
    ATR Phase Shift 탐지
    
    Returns:
        float: 0.0 (정상) ~ 1.0 (M&A 의심)
    """
    # === 1. 급등/급락 이벤트 탐지 ===
    event_idx = None
    for i in range(5, lookback):  # 최근 5~40일 범위
        daily_change = abs(data[-i]['close'] - data[-i]['open']) / data[-i]['open']
        if daily_change > 0.25:  # 25% 이상 변동
            event_idx = i
            break
    
    if event_idx is None:
        return 0.0  # 이벤트 없음 → 정상
    
    # === 2. 기존 ATR 계산 (이벤트 이전 10일) ===
    pre_event_start = event_idx + 10
    pre_event_end = event_idx + 1
    pre_atr = calc_atr(data[-pre_event_start:-pre_event_end])
    
    # === 3. 이후 ATR 계산 (최근 5일) ===
    post_atr = calc_atr(data[-5:])
    
    # === 4. ATR Collapse Ratio ===
    # 이벤트 후 ATR이 급격히 감소하면 → M&A 의심
    if pre_atr <= 0:
        return 0.0
    
    collapse_ratio = 1.0 - (post_atr / pre_atr)
    
    # 70% 이상 감소 시 강한 의심
    if collapse_ratio > 0.7:
        return 1.0
    elif collapse_ratio > 0.5:
        return 0.7
    elif collapse_ratio > 0.3:
        return 0.3
    
    return 0.0


def calc_atr(data, period=None):
    """ATR 계산 헬퍼"""
    if not data:
        return 0.0
    tr_list = []
    for i in range(1, len(data)):
        h_l = data[i]['high'] - data[i]['low']
        h_pc = abs(data[i]['high'] - data[i-1]['close'])
        l_pc = abs(data[i]['low'] - data[i-1]['close'])
        tr_list.append(max(h_l, h_pc, l_pc))
    return sum(tr_list) / len(tr_list) if tr_list else 0.0
```

**적용**:
```python
phase_shift = detect_atr_phase_shift(data)
if phase_shift > 0.5:
    score *= (1.0 - phase_shift * 0.5)  # 최대 50% 페널티
    tag = "🔒 Event Lock"  # UI 태그
```

**장점**:
- 순수 가격 데이터만 사용 (외부 의존 없음)
- 진정한 VCP와 M&A 종목 정밀 구분
- 점진적 페널티로 오탐 최소화

**단점**:
- 이벤트 발생 후 5~10일 경과해야 탐지 가능

---

### 3.3 Option C: 상승 여력 지표 추가

**개념**: ATH(All-Time High) 대비 현재 위치로 상승 여력 측정

```python
def upside_potential(data):
    """
    52주 최고가 대비 현재 위치
    0.9 이상 = 상승 여력 부족
    """
    high_52w = max(d['high'] for d in data[-252:])
    current = data[-1]['close']
    return current / high_52w

# 적용
potential = upside_potential(data)
if potential > 0.95:  # ATH 근처
    score *= (1.0 - potential)  # 페널티
```

**장점**: 간단한 로직  
**단점**: 일반 강세 종목도 페널티 받을 수 있음

---

### 3.4 Option D: 변동성 비대칭 분석

**개념**: 상승 변동성 vs 하락 변동성 비교

```python
def volatility_asymmetry(data, lookback=20):
    """
    M&A 종목: 하락 변동성 낮음 (인수가 지지)
    매집 종목: 상승/하락 변동성 대칭
    """
    up_moves = [d for d in data if d['close'] > d['open']]
    down_moves = [d for d in data if d['close'] < d['open']]
    
    avg_up = mean(abs(d['close'] - d['open']) for d in up_moves)
    avg_down = mean(abs(d['close'] - d['open']) for d in down_moves)
    
    if avg_down < avg_up * 0.3:  # 하락 변동 매우 작음
        return "MA_SUSPECTED"
    return "NORMAL"
```

---

## 4. 권장 구현 순서

| 순위 | 옵션 | 난이도 | 효과 |
|------|------|--------|------|
| 1 | Option B (가격 천장) | 중 | 🟢 높음 |
| 2 | Option D (변동성 비대칭) | 중 | 🟢 높음 |
| 3 | Option C (상승 여력) | 하 | 🟡 중간 |
| 4 | Option A (이벤트 필터) | 상 | 🟢 높음 (데이터 있으면) |

---

## 5. 결론

> [!IMPORTANT]
> M&A/Merger Arbitrage 종목은 **"이미 폭발 완료 후 인수가 수렴"** 상태이지만,
> Score V3의 4가지 신호가 구조적으로 충족되어 **거짓 양성**으로 상위권에 노출됨.

**핵심 해결책**:  
가격 천장 감지 (Option B) + 변동성 비대칭 분석 (Option D) 조합 권장
