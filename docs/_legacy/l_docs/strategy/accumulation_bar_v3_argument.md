# Accumulation Bar V3 Zero-Return Problem: Analysis & Arguments

> **문서 유형**: 가상 토론 (Hypothetical Argument)  
> **작성일**: 2026-01-06  
> **관련 알고리즘**: `_calc_accumulation_bar_intensity_v3()`

---

## 문제 정의

Accumulation Bar V3 강도가 대부분의 종목에서 **0.00**을 반환한다.

```
SMXT:  Tight Range=0.62, OBV=0.51, AccumBar=0.00, VolDryout=0.38
AMCI:  Tight Range=0.55, OBV=0.48, AccumBar=0.00, VolDryout=0.41
RETO:  Tight Range=0.71, OBV=0.44, AccumBar=0.00, VolDryout=0.29
```

---

## 알고리즘 분석

### 현재 V3 로직
```python
# 1. 양봉 조건
is_bullish = close_price > open_price
if not is_bullish or ratio < 1.5:
    return 0.0  # ← 여기서 대부분 탈락

# 2. 로그 스케일
log_ratio = log(ratio) - log(1.5)  # 1.5x에서 시작
```

### 0.00 반환 조건
1. **음봉** (Close < Open) → 즉시 0.0
2. **거래량 < 1.5x** → 즉시 0.0
3. **양봉이지만 Body Ratio가 낮음** → 최종값 감소

---

## 🔴 Argument A: "이건 버그다"

### 주장
> Accumulation Bar의 발생 빈도가 너무 낮다. 20개 종목 중 0개가 0이 아닌 값을 가지면, 이 신호는 사실상 무용지물이다.

### 근거
1. **실용성 부재**: 대부분 0.00이면 가중치 20%가 낭비됨
2. **V2와의 괴리**: V2는 최소한 "작은 값"이라도 반환했음
3. **설계 의도 위배**: 매집봉은 "큰 거래량 + 작은 변동"인데, V3는 "양봉"까지 요구

### 제안
```python
# 양봉 조건 완화
is_bullish = close_price >= open_price  # 보합 허용
if ratio < 1.2:  # 1.5x → 1.2x로 낮춤
    return 0.0
```

---

## 🟢 Argument B: "이건 정상이다"

### 주장
> 0.00이 많다는 것은 "진짜 매집봉이 없다"는 의미다. 신호가 희소해야 가치가 있다.

### 근거
1. **희소성이 곧 가치**: 모든 종목이 높은 점수를 받으면 필터링 의미 상실
2. **양봉 조건의 타당성**: 매집봉은 "조용히 사들이는 것"이므로, 음봉은 매집이 아니라 투매
3. **1.5x 기준의 타당성**: 평균의 1.5배 미만은 일상적 변동 범위

### 반박
> "20개 종목 모두 0.00"은 문제가 아니라, **오늘 진정한 매집봉이 없다**는 사실의 반영이다.

---

## 🟡 Argument C: "설계는 맞지만, 튜닝이 필요하다"

### 주장
> 알고리즘 구조는 유지하되, 파라미터를 조정해야 한다.

### 제안 옵션

| 파라미터 | 현재값 | 제안값 | 근거 |
|---------|-------|-------|------|
| 거래량 배수 기준 | 1.5x | 1.3x | 마이크로캡은 거래량 변동이 심함 |
| 양봉 조건 | Close > Open | Close ≥ Open | 보합 캔들도 매집일 수 있음 |
| Body Ratio 곱하기 | 적용 | 제거 | 이중 페널티 방지 |

### 절충안 코드
```python
# 보합 허용 + 1.3x 기준
is_bullish = close_price >= open_price
if ratio < 1.3:
    return 0.0

# Body Ratio는 선택적 가중 (0.5 ~ 1.0 범위로 클램핑)
body_ratio = max(0.5, abs(close - open) / (high - low))
```

---

## 🔵 Argument D: "Base 0.5 + 가감점 구조" ⭐ 채택

### 주장
> 0.5를 기준점으로 두고, 부정적 신호는 감점(-), 긍정적 신호는 가산점(+)으로 처리한다.
> "희소하지만 정확한 신호"의 가치를 유지하면서, 더블 페널티도 명시적으로 표현한다.

### 핵심 아이디어

```
기존: 신호 없음 → 0.00 (무시됨)
개선: 신호 없음 → 0.50 (중립)
     페널티 조건 → 0.50 미만 (감점)
     신호 조건   → 0.50 초과 (가산점)
```

### 장점
1. **희소성 유지**: 진짜 매집봉만 0.5 이상 획득 (어드벤티지)
2. **더블 페널티 표현**: 음봉+저거래량 → 0.5 미만으로 실제 감점
3. **20% 가중치 활용**: 모든 종목이 0.5 기준으로 비교 가능

### 제안 알고리즘

```python
def _calc_accumulation_bar_intensity_v3(self, data: Any) -> float:
    """
    Base 0.5 + 가감점 구조
    
    Returns:
        float: 0.0 ~ 1.0 (0.5 = 중립)
    """
    BASE_SCORE = 0.5
    
    latest = data[-1]
    open_price, close_price = latest["open"], latest["close"]
    high_price, low_price = latest["high"], latest["low"]
    current_volume = latest["volume"]
    
    prev_avg_vol = mean(volumes[:-1])
    ratio = current_volume / prev_avg_vol
    
    # === 가감점 계산 ===
    adjustment = 0.0
    
    # 1. 양봉/음봉 (±0.15)
    if close_price > open_price:
        adjustment += 0.15  # 양봉 보너스
    elif close_price < open_price:
        adjustment -= 0.15  # 음봉 페널티
    
    # 2. 거래량 배수 (±0.25)
    if ratio >= 1.5:
        # 로그 스케일 보너스 (1.5x → +0.05, 4x → +0.25)
        log_bonus = min(0.25, (log(ratio) - log(1.5)) / (log(4) - log(1.5)) * 0.25)
        adjustment += log_bonus
    elif ratio < 0.8:
        adjustment -= 0.15  # 거래량 급감 페널티
    
    # 3. Body Ratio (±0.10)
    if high_price != low_price:
        body_ratio = abs(close_price - open_price) / (high_price - low_price)
        if body_ratio >= 0.6:
            adjustment += 0.10  # 실체 큰 캔들 보너스
        elif body_ratio < 0.2:
            adjustment -= 0.10  # 도지/위꼬리 페널티
    
    # === 최종 점수 ===
    intensity = max(0.0, min(1.0, BASE_SCORE + adjustment))
    return round(intensity, 2)
```

### 예상 결과

| 조건 | 가감점 | 최종 점수 | 해석 |
|------|-------|----------|------|
| 양봉 + 2x 거래량 + 실체 큼 | +0.15 +0.15 +0.10 | **0.90** | 🔥 강력한 매집봉 |
| 양봉 + 1.5x 거래량 | +0.15 +0.05 | **0.70** | ✅ 매집 신호 |
| 보합 + 평균 거래량 | 0 | **0.50** | ➖ 중립 |
| 음봉 + 저거래량 + 도지 | -0.15 -0.15 -0.10 | **0.10** | ⚠️ 경고 신호 |
| 음봉 + 급감 거래량 | -0.15 -0.15 | **0.20** | 🚨 더블 페널티 |

### 왜 이 방식이 좋은가?

1. **희소성 유지**: 0.7 이상은 여전히 희귀 (진짜 매집봉만)
2. **페널티 표현 가능**: 0.5 미만 = 실제로 점수 깎임
3. **비교 가능성**: "이 종목은 AccumBar 0.3이고, 저 종목은 0.7이다" 라는 비교가 의미 있음
4. **가중치 활성화**: 20% 가중치가 모든 종목에서 영향력 발휘

---

## 결론: 어떤 관점을 취할 것인가?

| 관점 | 결론 | 리스크 | 채택 |
|------|------|--------|------|
| A. 버그 | 조건 완화 | 노이즈 증가 | ❌ |
| B. 정상 | 현행 유지 | 20% 가중치 무효 | ❌ |
| C. 튜닝 | 파라미터 조정 | 백테스트 필요 | ❌ |
| **D. Base 0.5** | **가감점 구조** | **설계 변경 필요** | ⭐ **채택** |

---

## ⚠️ 반론: Dryout과의 모순

### 문제 발견
> AccumBar에서 "거래량 급감 페널티"를 주면, Volume Dryout과 상쇄된다!

| 거래량 상황 | AccumBar (제안) | Volume Dryout | 총합 |
|------------|----------------|---------------|------|
| 급감 (0.5x) | -0.15 (페널티) | +0.8 (좋음) | 상쇄 |
| 급증 (3x) | +0.25 (보너스) | -0.5 (나쁨) | 상쇄 |

### 근본 원인
**두 지표가 같은 변수(거래량)를 반대 방향으로 해석**
- Dryout: 거래량↓ = 준비 중 (좋음)
- AccumBar: 거래량↓ = 매집 없음 (나쁨)

### 해결책: 역할 분리

```
Volume Dryout → "폭풍 전 고요" (거래량 고갈 탐지)
Accumulation Bar → "조용한 매집" (작은 변동 + 양봉 탐지)
                    ※ 거래량은 판단하지 않음!
```

### 수정된 AccumBar 알고리즘

```python
def _calc_accumulation_bar_intensity_v3(self, data: Any) -> float:
    """
    역할: 양봉 + 작은 변동 = 조용한 매집
    ※ 거래량은 Volume Dryout이 담당하므로 여기서는 제외
    """
    BASE_SCORE = 0.5
    
    latest = data[-1]
    open_price, close_price = latest["open"], latest["close"]
    high_price, low_price = latest["high"], latest["low"]
    
    adjustment = 0.0
    
    # 1. 양봉/음봉 (±0.20)
    if close_price > open_price:
        adjustment += 0.20
    elif close_price < open_price:
        adjustment -= 0.20
    
    # 2. 변동폭 (±0.15) - 작을수록 좋음
    if high_price != low_price and close_price > 0:
        range_pct = (high_price - low_price) / close_price
        if range_pct < 0.02:  # 2% 미만 = 매우 조용
            adjustment += 0.15
        elif range_pct > 0.05:  # 5% 초과 = 변동 큼
            adjustment -= 0.10
    
    # 3. Body Ratio (±0.15) - 실체 클수록 좋음
    if high_price != low_price:
        body_ratio = abs(close_price - open_price) / (high_price - low_price)
        if body_ratio >= 0.7:
            adjustment += 0.15
        elif body_ratio < 0.3:
            adjustment -= 0.15
    
    # ※ 거래량은 판단하지 않음 (Dryout과 역할 분리)
    
    return round(max(0.0, min(1.0, BASE_SCORE + adjustment)), 2)
```

### 수정된 예상 결과

| 조건 | 가감점 | 점수 | 해석 |
|------|-------|------|------|
| 양봉 + 변동↓ + 실체↑ | +0.20+0.15+0.15 | **1.00** | 🔥 완벽한 매집봉 |
| 양봉 + 보통 | +0.20 | **0.70** | ✅ 매집 신호 |
| 보합 | 0 | **0.50** | ➖ 중립 |
| 음봉 + 변동↑ + 도지 | -0.20-0.10-0.15 | **0.05** | 🚨 경고 |

### 역할 분리 요약

| 지표 | 관찰 대상 | 좋은 신호 |
|------|---------|----------|
| **Tight Range** | ATR 변화 | 변동성 수축 |
| **OBV Divergence** | 가격 vs OBV | 가격↓ + OBV↑ |
| **Volume Dryout** | 거래량 | 거래량 급감 + 가격 지지 |
| **Accumulation Bar** | 캔들 모양 | 양봉 + 작은 변동 + 실체 큼 |

> 💡 **핵심**: 각 지표가 **서로 다른 변수**를 관찰해야 상쇄되지 않는다.

---

## � 시간적 관계: AccumBar → Dryout 시퀀스

### 문제 발견
> 현재 설계는 AccumBar와 Dryout 모두 **"오늘"** 기준으로 계산한다.
> 하지만 매집 패턴은 **시간 순서**가 있다!

### 매집의 타임라인

```
     과거                                      현재
     ├────────────────────────────────────────────┤
     │                                            │
     │  [AccumBar 기간]      [Dryout 기간]        │
     │  (세력이 매집)        (거래량 고갈)        │
     │                                            │
     Day -20        Day -10    Day -5     Day 0
                         └──────┬──────┘
                                │
                         Ignition 임박!
```

### 🔥 핵심 통찰

| 단계 | 기간 | 현상 | 의미 |
|------|------|------|------|
| **1. 매집** | Day -20 ~ -10 | 양봉 + 작은 변동 | 세력이 조용히 사들임 |
| **2. 고갈** | Day -5 ~ 0 | 거래량 급감 | 매도세 소진, 매물 고갈 |
| **3. 폭발** | Day 0 ~ +? | 급등 | Ignition! |

### 수정된 설계

```python
# ❌ 기존 설계 (둘 다 최근 1일)
AccumBar: data[-1]  # 오늘만
Dryout:   data[-5:-1] / data[-20:]  # 최근 5일

# ✅ 수정 설계 (시간적 분리)
AccumBar: data[-15:-5]  # 10~5일 전 (과거 매집 기간)
Dryout:   data[-5:]     # 최근 5일 (현재 고갈 기간)
```

### 수정된 AccumBar 알고리즘

```python
def _calc_accumulation_bar_intensity_v3(self, data: Any) -> float:
    """
    역할: 과거 매집 기간(10~5일 전)의 캔들 패턴 분석
    ※ Dryout(최근 5일)과 시간 분리
    """
    BASE_SCORE = 0.5
    
    # 과거 매집 기간: 10~5일 전
    if len(data) < 15:
        return BASE_SCORE  # 데이터 부족 시 중립
    
    accum_period = data[-15:-5]  # 10일 전 ~ 5일 전
    
    # 해당 기간 동안의 양봉 비율
    bullish_count = sum(1 for d in accum_period if d["close"] > d["open"])
    bullish_ratio = bullish_count / len(accum_period)
    
    # 해당 기간 평균 변동폭
    avg_range = mean((d["high"] - d["low"]) / d["close"] for d in accum_period)
    
    # 해당 기간 평균 Body Ratio
    body_ratios = []
    for d in accum_period:
        if d["high"] != d["low"]:
            br = abs(d["close"] - d["open"]) / (d["high"] - d["low"])
            body_ratios.append(br)
    avg_body_ratio = mean(body_ratios) if body_ratios else 0.5
    
    # === 가감점 계산 ===
    adjustment = 0.0
    
    # 1. 양봉 비율 (±0.20)
    if bullish_ratio >= 0.7:      # 70% 이상 양봉
        adjustment += 0.20
    elif bullish_ratio <= 0.3:    # 30% 이하 양봉 (대부분 음봉)
        adjustment -= 0.20
    
    # 2. 변동폭 (±0.15)
    if avg_range < 0.02:          # 평균 2% 미만
        adjustment += 0.15
    elif avg_range > 0.05:        # 평균 5% 초과
        adjustment -= 0.10
    
    # 3. Body Ratio (±0.15)
    if avg_body_ratio >= 0.6:     # 평균 60% 이상
        adjustment += 0.15
    elif avg_body_ratio < 0.3:    # 평균 30% 미만
        adjustment -= 0.15
    
    return round(max(0.0, min(1.0, BASE_SCORE + adjustment)), 2)
```

### 시간 분리된 지표 요약

| 지표 | 관찰 기간 | 관찰 대상 | 좋은 신호 |
|------|----------|----------|----------|
| **Tight Range** | 5일 vs 20일 | ATR 변화 | 변동성 수축 |
| **OBV Divergence** | 20일 | 가격 vs OBV | 가격↓ + OBV↑ |
| **Accumulation Bar** | **10~5일 전** | 캔들 모양 | 양봉 + 작은 변동 |
| **Volume Dryout** | **최근 5일** | 거래량 | 거래량 급감 |

### 이제 상쇄되지 않는 이유

```
시나리오: 이상적인 매집 패턴

Day -15 ~ -5:  AccumBar 기간
  → 양봉 80%, 변동 1.5%, 실체 65%
  → AccumBar = 0.5 + 0.20 + 0.15 + 0.15 = 1.00 🔥

Day -5 ~ 0:  Dryout 기간
  → 거래량 20일 평균의 30%
  → Dryout = 0.85 🔥

둘 다 높음 = 폭발 임박!
```

> 💡 **핵심**: AccumBar와 Dryout은 **서로 다른 시기**를 관찰하므로, 
> 동시에 높은 점수가 나올 수 있고, 이것이 진정한 "폭발 임박" 신호다.

---

## 📐 Float 기반 동적 Dryout 기간

### 왜 Float인가?
- Dryout = "매물 고갈"
- Float = "실제 거래 가능한 주식 수"
- **Float이 작을수록 → 매물이 빨리 고갈** (직접적 인과관계)

### 공식

```python
def get_dryout_days(float_shares: int) -> int:
    """Float 기반 동적 Dryout 기간 계산"""
    # 기본 3일 + Float 300만주당 1일 추가 (최대 10일)
    return min(10, max(3, 3 + float_shares // 3_000_000))
```

### 예시

| Float | Dryout 기간 | 이유 |
|-------|------------|------|
| 3M | 4일 | 매물이 빨리 고갈됨 |
| 6M | 5일 | 중간 |
| 12M | 7일 | 큰 편 |
| 15M+ | 10일 | 대형 float, 고갈 오래 걸림 |

### AccumBar 기간도 연동

```python
def get_accumbar_period(float_shares: int) -> tuple[int, int]:
    """Float 기반 AccumBar 관찰 기간"""
    dryout_days = get_dryout_days(float_shares)
    # AccumBar는 Dryout 시작 전 2주 정도
    start = dryout_days + 10  # Dryout 시작점 + 10일
    end = dryout_days         # Dryout 시작점
    return (start, end)  # 예: 3M float → (14, 4)
```

---

## 🔄 거래량 재도입: 시간 분리로 가능해짐

### 이전 문제 (같은 시기)
```
AccumBar: data[-1] → 거래량↑ 좋음
Dryout:   data[-5:] → 거래량↓ 좋음
→ 상쇄!
```

### 현재 (다른 시기)
```
AccumBar: data[-15:-5] → 거래량↑ 좋음 (세력이 활발히 매집)
Dryout:   data[-5:]    → 거래량↓ 좋음 (매물 고갈)
→ 보완적!
```

### 매집 시퀀스 (거래량 포함)

| 단계 | 기간 | 거래량 | 캔들 | 신호 |
|------|------|--------|------|------|
| **1. 매집** | Day -15 ~ -5 | 📈 높음 | 양봉 + 작은 변동 | AccumBar HIGH |
| **2. 고갈** | Day -5 ~ 0 | 📉 낮음 | 횡보 | Dryout HIGH |
| **3. 폭발** | Day 0+ | 🚀 급증 | 급등 | Ignition! |

### 수정된 AccumBar 알고리즘 (거래량 재도입)

```python
def _calc_accumulation_bar_intensity_v3(self, data: Any, float_shares: int = 10_000_000) -> float:
    """
    과거 매집 기간의 캔들 패턴 + 거래량 분석
    ※ Dryout과 시간 분리되어 거래량 재도입 가능
    """
    BASE_SCORE = 0.5
    
    # 동적 기간 계산
    dryout_days = min(10, max(3, 3 + float_shares // 3_000_000))
    accum_start = dryout_days + 10
    accum_end = dryout_days
    
    if len(data) < accum_start:
        return BASE_SCORE
    
    accum_period = data[-accum_start:-accum_end]
    
    adjustment = 0.0
    
    # 1. 양봉 비율 (±0.15)
    bullish_ratio = sum(1 for d in accum_period if d["close"] > d["open"]) / len(accum_period)
    if bullish_ratio >= 0.7:
        adjustment += 0.15
    elif bullish_ratio <= 0.3:
        adjustment -= 0.15
    
    # 2. 변동폭 (±0.15)
    avg_range = mean((d["high"] - d["low"]) / d["close"] for d in accum_period)
    if avg_range < 0.02:
        adjustment += 0.15
    elif avg_range > 0.05:
        adjustment -= 0.10
    
    # 3. Body Ratio (±0.10)
    body_ratios = [abs(d["close"] - d["open"]) / (d["high"] - d["low"]) 
                   for d in accum_period if d["high"] != d["low"]]
    avg_body = mean(body_ratios) if body_ratios else 0.5
    if avg_body >= 0.6:
        adjustment += 0.10
    elif avg_body < 0.3:
        adjustment -= 0.10
    
    # 4. 거래량 (±0.10) ← 재도입!
    # 매집 기간 평균 vs 전체 평균
    accum_vol = mean(d["volume"] for d in accum_period)
    total_vol = mean(d["volume"] for d in data)
    if accum_vol > total_vol * 1.3:  # 30% 이상 높으면
        adjustment += 0.10  # 활발한 매집
    elif accum_vol < total_vol * 0.7:  # 30% 이상 낮으면
        adjustment -= 0.10  # 매집 활동 없음
    
    return round(max(0.0, min(1.0, BASE_SCORE + adjustment)), 2)
```

### 최종 점수 분포

| 조건 | 가감점 | 점수 | 해석 |
|------|-------|------|------|
| 양봉 70%+ 변동↓ 실체↑ 거래량↑ | +0.15+0.15+0.10+0.10 | **1.00** | 🔥 완벽한 매집 |
| 양봉 60% 보통 | +0.10 | **0.60** | ✅ 매집 가능성 |
| 보합 | 0 | **0.50** | ➖ 중립 |
| 음봉 70%+ 변동↑ 거래량↓ | -0.15-0.10-0.10 | **0.15** | 🚨 투매/하락 |

> 💡 **결론**: 시간 분리 덕분에 거래량을 다시 도입할 수 있으며,
> 이제 AccumBar(매집 기간 고거래량)와 Dryout(고갈 기간 저거래량)이 **보완적**으로 작동한다.

---

## 🛡️ 이상치 내성 (Outlier Robustness)

### 문제
> 10일 중 1~2일 "발작"(급등락)이 있으면 평균이 왜곡된다.

### 해결: 비율 + Median 혼합

```python
def _calc_accumulation_bar_intensity_v3(self, data, float_shares=10_000_000):
    """이상치에 강건한 매집봉 탐지"""
    BASE_SCORE = 0.5
    
    # 동적 기간
    dryout_days = min(10, max(3, 3 + float_shares // 3_000_000))
    accum_start, accum_end = dryout_days + 10, dryout_days
    
    if len(data) < accum_start:
        return BASE_SCORE
    
    period = data[-accum_start:-accum_end]
    n = len(period)
    
    adjustment = 0.0
    
    # 1. 양봉 비율 (이미 robust)
    bullish_ratio = sum(1 for d in period if d["close"] > d["open"]) / n
    if bullish_ratio >= 0.7: adjustment += 0.15
    elif bullish_ratio <= 0.3: adjustment -= 0.15
    
    # 2. 변동폭 → "조용한 날 비율"로 변경 (1~2일 발작 허용)
    quiet_days = sum(1 for d in period if (d["high"]-d["low"])/d["close"] < 0.03)
    if quiet_days / n >= 0.7: adjustment += 0.15  # 70% 이상 조용
    elif quiet_days / n < 0.3: adjustment -= 0.10  # 30% 미만 조용
    
    # 3. Body Ratio → Median 사용 (이상치 무시)
    body_ratios = [abs(d["close"]-d["open"])/(d["high"]-d["low"]) 
                   for d in period if d["high"] != d["low"]]
    body_median = sorted(body_ratios)[len(body_ratios)//2] if body_ratios else 0.5
    if body_median >= 0.6: adjustment += 0.10
    elif body_median < 0.3: adjustment -= 0.10
    
    # 4. 거래량 → Median 사용 (하루 폭발해도 무시)
    accum_vols = [d["volume"] for d in period]
    total_vols = [d["volume"] for d in data]
    accum_median = sorted(accum_vols)[len(accum_vols)//2]
    total_median = sorted(total_vols)[len(total_vols)//2]
    if accum_median > total_median * 1.3: adjustment += 0.10
    elif accum_median < total_median * 0.7: adjustment -= 0.10
    
    return round(max(0.0, min(1.0, BASE_SCORE + adjustment)), 2)
```

### 왜 Robust한가?

| 요소 | 방법 | 효과 |
|------|------|------|
| 양봉 비율 | 카운팅 | 1~2일 음봉 OK (70% 기준) |
| 변동폭 | "조용한 날 비율" | 1~2일 급변 OK |
| Body Ratio | Median | 이상치 무시 |
| 거래량 | Median | 하루 폭발 무시 |

---

## ⚡ 성능 분석

### 계산 복잡도

| 연산 | n | 복잡도 | 대략적 연산 수 |
|------|---|--------|---------------|
| 캔들 순회 | 10 | O(n) | 10 |
| 비율 계산 | 10 | O(n) | 10 |
| Median (정렬) | 10 | O(n log n) | 40 |
| 총 1종목 | - | - | **~100** |

### 실제 부하

```
50종목 × 100연산 = 5,000 연산/주기
1분 재계산 = 5,000 연산/분 = 83 연산/초

현대 CPU: ~10,000,000,000 연산/초
→ 0.000001% 부하 (무시 가능)
```

> ✅ **결론**: 복잡해 보이지만 실제 부하는 **0.001ms 미만**. 전혀 문제 없음.

---

## �📖 세력의 매집을 캔들 모양으로 포착하는 원리

### 왜 "양봉 + 작은 변동 + 큰 실체"가 매집 신호인가?

세력(기관, 큰손)이 주식을 매집할 때는 **가격을 올리지 않으면서 조용히 사들이려** 합니다.
이 행동이 캔들 차트에 어떻게 나타나는지 단계별로 살펴보겠습니다.

---

### 1️⃣ 세력의 딜레마

```
목표: 100만주를 사고 싶다
문제: 한 번에 사면 가격이 급등해서 평균 매입가가 높아진다
해결: 조금씩, 여러 날에 걸쳐, 가격을 자극하지 않고 산다
```

### 2️⃣ 매집 시 나타나는 캔들 패턴

| 시장 상황 | 세력의 행동 | 캔들 모양 | AccumBar 반응 |
|---------|-----------|----------|---------------|
| 아침에 매도세 | 세력이 조용히 매수 받음 | 아래꼬리 긴 양봉 | 양봉 + 변동↓ = **높음** |
| 종일 횡보 | 세력이 지정가 매수 | 변동폭 작은 양봉 | 양봉 + 변동↓ = **높음** |
| 급락 후 반등 | 세력이 저점 매집 | 긴 아래꼬리 도지 | 양봉 아님 = **낮음** |
| 급등 후 음봉 | 개미가 추격매수, 세력은 관망 | 큰 변동 음봉 | 음봉 + 변동↑ = **매우 낮음** |

---

### 3️⃣ 각 요소가 의미하는 것

#### 🟢 양봉 (Close > Open)
```
의미: 하루 동안 순매수가 우세했다
세력 관점: 세력이 "받치고" 있다
개미 관점: 매수세가 매도세보다 강하다
```
- 세력은 가격을 "방어"하면서 매집합니다
- 시가보다 종가가 높다 = 누군가 꾸준히 사들였다

#### 🟢 작은 변동폭 (Range < 2%)
```
의미: 하루 동안 가격이 크게 움직이지 않았다
세력 관점: 가격을 자극하지 않고 성공적으로 매집
개미 관점: 조용한 하루 (관심 없음)
```
- 변동폭이 작다 = 세력이 "조용히" 매집 중
- 변동폭이 크다 = 시장에 이벤트 발생 (뉴스, 급등락)

#### 🟢 큰 실체 (Body Ratio > 70%)
```
의미: 꼬리가 짧고 몸통이 길다
세력 관점: 일관된 방향성 (의도적)
개미 관점: 매수세가 종일 유지됐다
```
- 긴 위꼬리 = 매수 후 매도세에 밀림 (❌ 불안정)
- 긴 아래꼬리 = 매도 후 반등 (⚠️ 불확실)
- 짧은 꼬리 + 긴 몸통 = 일관된 매수세 (✅ 의도적)

---

### 4️⃣ 이상적인 매집봉 예시

```
      ┌───┐ High = 10.05
      │   │
      │ ▓ │ Close = 10.02  ← 양봉
      │ ▓ │
      │ ▓ │
      │ ▓ │ Open = 9.98
      └───┘ Low = 9.95

변동폭: (10.05 - 9.95) / 10.00 = 1.0% ✓ (작음)
양봉: 10.02 > 9.98 ✓
Body Ratio: (10.02 - 9.98) / (10.05 - 9.95) = 40% ⚠️ (중간)
```

→ AccumBar = 0.5 + 0.20(양봉) + 0.15(변동↓) = **0.85** ✅

---

### 5️⃣ 경고 신호 예시 (매집 아님)

```
      ┌───┐ High = 10.50
      │   │
      │   │
      │ ▓ │ Open = 10.30
      │ ▓ │
      │ ▓ │ Close = 9.80  ← 음봉
      │   │
      └───┘ Low = 9.70

변동폭: (10.50 - 9.70) / 10.00 = 8.0% ✗ (큼)
음봉: 9.80 < 10.30 ✗
```

→ AccumBar = 0.5 - 0.20(음봉) - 0.10(변동↑) = **0.20** 🚨

---

### 6️⃣ 왜 거래량을 제외했는가?

| 관점 | 거래량이 필요한가? |
|------|------------------|
| ❌ V1 설계 | "매집봉 = 큰 거래량 + 작은 변동" |
| ✅ V3 수정 | "매집봉 = 양봉 + 작은 변동" (거래량은 Dryout 담당) |

**이유**: 
- 세력은 **거래량을 숨기고** 싶어합니다
- Dark Pool, Block Trading 등으로 거래량이 안 보일 수 있음
- 따라서 **캔들 모양**만으로 "조용한 매집"을 탐지하는 것이 더 정확함

---

### 7️⃣ 최종 해석

```
AccumBar ≥ 0.8  → "세력이 조용히 매집 중일 가능성 높음"
AccumBar = 0.5  → "중립, 특별한 신호 없음"
AccumBar ≤ 0.2  → "세력 매집 아님, 투매 또는 변동성 장세"
```

> 💡 **결론**: 거래량 없이 캔들 모양(양봉/변동폭/실체)만으로도 
> **"조용하고 일관된 매수세"**를 탐지할 수 있으며, 
> 이것이 세력 매집의 핵심 패턴입니다.

## 후속 조치 제안

1. ~~데이터 수집~~
2. ~~백테스트~~
3. **구현**: `_calc_accumulation_bar_intensity_v3()` 재작성
4. **검증**: 실제 종목에서 0.5 기준 분포 확인

---

> 💡 **최종 결정**: **"희소하지만 정확한 신호"**를 유지하되, **Base 0.5 + 가감점 구조**로 더블 페널티와 어드벤티지를 모두 표현한다.
