# test_finplot_embed.py

## 기본 정보
| 항목 | 값 |
|------|---|
| **경로** | `tests/test_finplot_embed.py` |
| **역할** | finplot 라이브러리의 PyQt6 위젯 임베딩 테스트 |
| **라인 수** | 114 |

## 함수

| 함수명 | 시그니처 | 설명 |
|--------|----------|------|
| `create_sample_data` | `(n=100) -> pd.DataFrame` | 테스트용 OHLCV 샘플 데이터 생성 |

## 클래스

### `TestWindow(QMainWindow)`
> finplot 차트 임베딩 테스트를 위한 PyQt6 윈도우

| 메서드 | 시그니처 | 설명 |
|--------|----------|------|
| `__init__` | `(self)` | 윈도우 초기화 및 finplot 차트 위젯 생성 |
| `load_data` | `(self)` | 캔들스틱 + 볼륨 차트에 데이터 로드 |

## 테스트 내용

### finplot 임베딩 패턴
> `fplt.create_plot_widget()` 사용 방식 검증

| 항목 | 구현 |
|------|------|
| **Master Widget** | `central.axs = []` 속성 필요 (finplot 요구사항) |
| **차트 생성** | `fplt.create_plot_widget(master, rows=2)` |
| **이벤트 루프** | `fplt.show(qt_exec=False)` - Qt 앱에서 제어 |

### 차트 설정
| 설정 | 값 |
|------|---|
| **배경색** | `#1a1a2e` (Dark) |
| **전경색** | `#e0e0e0` (Light Grey) |
| **상승 캔들** | `#22c55e` (Green) |
| **하락 캔들** | `#ef4444` (Red) |

## 🔗 외부 연결 (Connections)

### Imports From
| 파일 | 가져오는 항목 |
|------|--------------| 
| (N/A - 표준 테스트 스크립트) | - |

### 관련 프로덕션 코드
| 파일 | 관계 |
|------|------|
| `frontend/gui/chart/finplot_chart.py` | 이 테스트 패턴 기반 프로덕션 구현 |

## 외부 의존성
- `finplot` - 금융 차트 라이브러리
- `PyQt6` - GUI 프레임워크
- `pandas` - 데이터프레임
- `numpy` - 샘플 데이터 생성
