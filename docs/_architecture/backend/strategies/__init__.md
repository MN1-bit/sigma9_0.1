# __init__.py

## 기본 정보

| 항목 | 값 |
|------|---|
| **경로** | `backend/strategies/__init__.py` |
| **역할** | 전략 플러그인 패키지 초기화 - 순환 import 방지를 위한 최소 export |
| **라인 수** | 38 |
| **바이트** | 1,424 |

## 내용 요약

> 📌 전략 추가 가이드 및 패키지 구조 정의
> 순환 import 방지를 위해 직접 export 없이 서브모듈에서 명시적 import 권장

```python
__all__ = [
    "seismograph",  # 서브패키지
]
```

## 포함 전략

| 전략 | 설명 |
|------|------|
| `_template.py` | 새 전략 개발 템플릿 (복사용, `_` prefix로 StrategyLoader 무시) |
| `seismograph/` | 메인 전략 패키지 (Seismograph Strategy) |

## 🔗 외부 연결 (Connections)

### Imports From (이 파일이 가져오는 것)
| 파일 | 가져오는 항목 |
|------|--------------|
| (없음) | 순환 import 방지를 위해 직접 import 제거됨 |

### Imported By (이 파일을 가져가는 것)
| 파일 | 사용 목적 |
|------|----------|
| (간접) | `from backend.strategies.seismograph import ...` 형태로 사용 |

## 사용법

```python
# 권장: 서브모듈에서 직접 import
from backend.strategies.seismograph import SeismographStrategy
```

## 외부 의존성
- (없음)
