# __init__.py

## 기본 정보

| 항목 | 값 |
|------|---|
| **경로** | `backend/__init__.py` |
| **역할** | Backend 패키지 초기화 마커 (빈 패키지) |
| **라인 수** | 2 |
| **바이트** | 19 |

## 내용

```python
# Backend package
```

> 📌 빈 `__init__.py`로, 다른 모듈에서 `backend.*` import를 가능하게 하는 패키지 마커 역할만 수행

## 🔗 외부 연결 (Connections)

### Imports From (이 파일이 가져오는 것)
| 파일 | 가져오는 항목 |
|------|--------------|
| (없음) | - |

### Imported By (이 파일을 가져가는 것)
| 파일 | 사용 목적 |
|------|----------|
| (간접) | Python 패키지 시스템에 의해 자동 로드 |

## 외부 의존성
- (없음)
