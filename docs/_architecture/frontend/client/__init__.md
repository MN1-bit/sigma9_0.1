# client/__init__.py

## 기본 정보
| 항목 | 값 |
|------|---|
| **경로** | `frontend/client/__init__.py` |
| **역할** | Client 패키지 초기화 (Backend 통신 담당) |
| **라인 수** | 31 |

## 패키지 개요

Backend 서버와의 통신을 담당하는 클라이언트 모듈 패키지입니다.

## 통신 패턴

| 프로토콜 | 라이브러리 | 용도 |
|----------|-----------|------|
| REST API | `httpx` | 상태 조회, 명령 전송 (동기) |
| WebSocket | `websockets` | 실시간 데이터 수신 (비동기 스트림) |

## Backend 엔드포인트

| 프로토콜 | URL 패턴 | 설명 |
|----------|----------|------|
| REST | `http://{host}:8000/api/...` | HTTP API |
| WebSocket | `ws://{host}:8000/ws/...` | 실시간 스트림 |

## 🔗 외부 연결 (Connections)

### 예정 모듈 (Step 5.x)
| 모듈 | 설명 |
|------|------|
| `APIClient` | REST API 클라이언트 |
| `WSClient` | WebSocket 클라이언트 |

> **참고**: 현재는 `frontend/services/` 하위의 `rest_adapter.py`, `ws_adapter.py`가 이 역할을 수행합니다.

## 외부 의존성
- `httpx` (REST)
- `websockets` (WebSocket)
